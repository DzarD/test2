import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    borderRadius: 10,
    borderWidth: 2,
    paddingLeft: 5,
    marginVertical: 10,
    padding: 10,
  },
});

export default styles;
